version: "3.9"

services:
  pdf-rag-app:
    build: .
    container_name: pdf-rag-chatbot
    ports:
      - "8501:8501"
    dns:
      - 1.1.1.1
      - 8.8.8.8
    env_file:
      - .env
    restart: unless-stopped
